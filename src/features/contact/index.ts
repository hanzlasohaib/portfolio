/**
 * Public API of the `contact` feature (docs/architecture/feature-template.md).
 * Pages should import from here rather than reaching into `components/`.
 *
 * TODO: Export the full `/contact` page (Contact Form, Success Message,
 * Validation — docs/project-design/pages.md § Contact) once implemented
 * in a later Phase 2 sprint. This sprint only implements the Home page
 * "Contact CTA" (`ContactSection`).
 */
export { ContactSection } from "./components/contact-section";
