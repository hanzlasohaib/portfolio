/**
 * Public API of the `about` feature (docs/architecture/feature-template.md).
 * Pages should import from here rather than reaching into `components/`.
 *
 * TODO: Export the full `/about` page content (Introduction, Professional
 * Summary, Education, Journey Summary, Skills, Technologies, Interests —
 * docs/project-design/pages.md § About) once that route is implemented in
 * a later Phase 2 sprint. This sprint only implements the Home page
 * "About Preview" section.
 */
export { AboutSection } from "./components/about-section";
