/**
 * Public API of the `home` feature (docs/architecture/feature-template.md).
 * Pages should import from here rather than reaching into `components/`.
 *
 * All other Home sections (About Preview, Featured Projects, Skills
 * Preview, Journey Timeline Preview, Contact CTA —
 * docs/project-design/pages.md § Home) are implemented in their own
 * feature modules (`features/about`, `features/projects`,
 * `features/skills`, `features/journey`, `features/contact`).
 */
export { HeroSection } from "./components/hero-section";
