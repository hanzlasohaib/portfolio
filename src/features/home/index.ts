/**
 * Public API of the `home` feature (docs/architecture/feature-template.md).
 * Pages should import from here rather than reaching into `components/`.
 *
 * TODO: Export the Contact CTA section once that feature is implemented
 * in a later Phase 2 sprint (docs/project-design/pages.md § Home). About
 * Preview, Featured Projects, Skills Preview, and Journey Timeline
 * Preview are implemented in their own feature modules (`features/about`,
 * `features/projects`, `features/skills`, `features/journey`).
 */
export { HeroSection } from "./components/hero-section";
