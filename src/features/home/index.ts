/**
 * Public API of the `home` feature (docs/architecture/feature-template.md).
 * Pages should import from here rather than reaching into `components/`.
 *
 * TODO: Export the Journey Timeline Preview and Contact CTA sections once
 * those features are implemented in later Phase 2 sprints
 * (docs/project-design/pages.md § Home). About Preview, Featured Projects,
 * and Skills Preview are implemented in their own feature modules
 * (`features/about`, `features/projects`, `features/skills`).
 */
export { HeroSection } from "./components/hero-section";
